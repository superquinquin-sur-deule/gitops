---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: adjust-ftop-seats
spec:
  schedule: "0 2 * * *"
  timeZone: Europe/Paris
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app.kubernetes.io/name: adjust-ftop-seats
        spec:
          restartPolicy: OnFailure
          containers:
            - name: odoo-cli
              image: docker.io/library/debian:bookworm-slim
              imagePullPolicy: IfNotPresent
              envFrom:
                - secretRef:
                    name: odoo-cli-secret
              command:
                - /bin/bash
                - -euo
                - pipefail
                - -c
                - |
                  apt-get update -qq
                  apt-get install -y -qq --no-install-recommends curl ca-certificates tzdata >/dev/null
                  export TZ=Europe/Paris
                  # renovate: datasource=github-releases depName=superquinquin-sur-deule/odoo-cli
                  version="0.13.0"
                  url="https://github.com/superquinquin-sur-deule/odoo-cli/releases/download/v$${version}/odoo-cli-$${version}-linux-x86_64"
                  curl -fsSL -o /usr/local/bin/odoo-cli "$url"
                  chmod +x /usr/local/bin/odoo-cli
                  begin="$(date +%d/%m/%Y)"
                  end="$(date -d '+28 days' +%d/%m/%Y)"
                  echo "Running adjust-ftop-seats from $begin to $end (include-draft)"
                  exec odoo-cli creneaux adjust-ftop-seats \
                    --begin-date "$begin" \
                    --end-date "$end" \
                    --include-draft
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  memory: 512Mi
